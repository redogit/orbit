# Decision Field Profile — Orbit Engine Fork

Canonical method: `redogit/redogit/DECISION_FIELD_CORE_2026-09-17.md`.

This profile is intentionally limited to the fork's engine/provenance surface. It does not turn the engine into evidence for unrelated research claims.

```text
X = current game-scene / game-object / engine state
O = declared engine behavior and provenance contract
D = consequential scene/object/update/render distinctions
R = containment, lifecycle, update, render-view and state-change relations
F = add/remove objects, update, transition scene/state, render through declared interface, verify contract
E = tests, workflow results, REDOGIT provenance and predecessor/upstream distinctions
G = bounded engine behavior/provenance contract satisfied
U = untested platform behavior, upstream parity, broader engine completeness
```

```text
FORK_PROVENANCE != UPSTREAM_PARITY
TESTED_INTERFACE != CROSS_PLATFORM_COMPLETENESS
ENGINE_STATE != RESEARCH_TRUTH
```
